<?xml version="1.0" encoding="UTF-8"?>
<local:BasePage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="FreshMvvmComplexNav.MainMenuPage"
		xmlns:local="clr-namespace:FreshMvvmComplexNav;assembly=FreshMvvmComplexNav" >
	<local:BasePage.Content>
		<TableView>
			<TableSection Title="Menu">     
                <TextCell Text="Contacts" Command="{Binding ShowContacts}"></TextCell>
                <TextCell Text="Quotes" Command="{Binding ShowQuotes}"></TextCell>
				<TextCell Text="Jump to Quote 1 (for demo)" Command="{Binding JumpToQuote}"></TextCell>
                <TextCell Text="Contact Us (Modal)" Command="{Binding ShowContactUs}"></TextCell>
				<TextCell Text="Logout" Command="{Binding Logout}"></TextCell>
            </TableSection>
		</TableView>
	</local:BasePage.Content>
</local:BasePage>
